package com.github.kdgaming0.skyrecipes.client.config;

/**
 * Controls when the search bar is visible in the RRV overlay.
 */
public enum SearchBarMode {
    ALWAYS,
    SHOW_WHEN_SEARCHING,
    DISABLED
}
